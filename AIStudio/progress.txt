================================================================================
SESSION: 2026-08-27 16:55
================================================================================

## 本次完成：MySQL 丢失后整站恢复 + 数据库重建迁移

### 背景
- 机器上的 F:\mysql（MySQL 8.0.29 便携版）已不存在，全盘（C/E/F）搜索无 mysqld.exe，
  Windows 无 MySQL 服务；后端强依赖 MySQL（application.yml 已从 SQLite 切到 MySQL），无法启动。
- 幸存的恢复源：data/racc.db（318MB SQLite 全量备份，含 23,821 篇知识库文档）+ tools/migration/SqliteToMysql（迁移工具源码）。

### 本次完成
- [x] 下载安装便携版 MySQL 8.0.29 → E:\mysql（华为云镜像 repo.huaweicloud.com，224MB，11.4MB/s；2026-08-27 平移至 E:\KjlStudy\mysql）
  - my.ini 重建于 E:\mysql\my.ini（utf8mb4 / ngram_token_size=2 / 端口3306 / max_allowed_packet=512M）
  - `--initialize-insecure` 初始化 → root 密码设 racc123 → 建库 racc(utf8mb4)
- [x] 后端首次启动（空库，Hibernate ddl-auto:update 自动建 39 张表 + KnowledgeDatabaseInitializer 建 FULLTEXT ngram 索引）
  然后停止后端 → SqliteToMysql 迁移（改 SRC_URL=E:/KjlStudy/AI/AIStudio/data/racc.db，javac 重新编译）
  → 38 表 / 24,112 行 / 14 秒全部迁入，knowledge_documents 23,821 行与 README 完全一致
- [x] Maven 重建后端：本机无 .m2（原构建在 Lenovo 用户下），配置阿里云镜像 ~/.m2/settings.xml 后
  `mvn -DskipTests package` 58.7s 成功，新 jar 含 mysql-connector-j-9.1.0
- [x] 全链路验证：登录 admin/admin123 ✓、/api/auth/me 鉴权 ✓、前端 8090 ✓、
  知识库列表（23,821 篇）✓、/api/knowledge/search?q=病历 FULLTEXT ngram 返回带评分结果 ✓
- [x] 启动/停止脚本更新（F:\AI管理平台 → E:\KjlStudy\AI\AIStudio，F:\mysql → E:\mysql）：
  启动平台.bat、停止平台.bat、tools/env.cmd、tools/env.sh、README.md 全部改到新路径
- [x] 平台现已运行：MySQL 3306 / 后端 8091（java -jar jar）/ 前端 8090（npm run dev）

## 当前状态
- 数据库：MySQL E:\KjlStudy\mysql:3306（root/racc123 库 racc），数据由 SQLite 全量迁移恢复
- 后端/前端运行于 8091 / 8090，登录 admin/admin123

## 下一步 / 备注
- 浏览器未能由会话自动打开（沙箱拒绝 start/explorer），用户需手动访问 http://localhost:8090
- 迁移工具编译依赖的 JDBC jar 备份在 tools/migration/lib/（mysql-connector-j-9.1.0 + sqlite-jdbc-3.46.1.3）
- 若 F:\mysql 重新出现，注意勿与 E:\mysql 双实例抢 3306
- LLM 网关 env.cmd 已带 WinCode 网关配置；embedding 端点网关不支持，平台自动降级关键词检索

## 遇到的问题
- Git Bash 下 PATH export 用 Windows 分号分隔符导致 mvn not found；应改用冒号或直接绝对路径调用
- 迁移工具 classpath 用 /tmp 虚拟路径 Windows JVM 不识别（No suitable driver）；需用真实 Windows 路径
- 中文文件名 bat 经 `cmd /c` 调用有编码问题；用户正常双击（Explorer 传 Unicode）无此问题

================================================================================
SESSION: 2026-08-27 19:30
================================================================================

## 本次完成：MySQL 平移 + MCP 需求服务 + 其他
- [x] MySQL 从 E:\mysql 平移至 E:\KjlStudy\mysql（优雅关闭→mv→改 my.ini basedir/datadir→重启）
  - 全量排查引用：仅 AIStudio 内 5 处（启动/停止 bat、README×4、progress.txt、application.yml 注释），
    PATH/服务/他处均无引用；后端 JDBC 走 localhost:3306 不依赖路径，Hikari 自动重连无需重启后端
- [x] 新建 MCP Server：data/mcp/mcp-tfs-requirements（TFS 需求获取）
  - 工具：query_requirements / get_requirement / get_requirement_tree / list_projects
  - 复用并升级 tfs-client.mjs：WIQL 改原生 POST /_apis/wit/wiql?api-version=4.1（SDK 走 GET 会 URL 过长 414；
    返回 15 万+ 条 id 时须先截断再 getWorkItems，否则一次 GET 携带海量 id 同样 414）
  - 需求树用原生 REST $expand=relations，绕开 SDK getWorkItem(depth) 在老 TFS 上超时的问题
  - stdio 直连测试通过：真实查询 WiNEX-Inpatient-2（total=152,779 条需求）✓
  - 已在平台注册并启动（id=2，RUNNING，toolCount=4，命令 C:/Program Files/nodejs/node.exe index.js）
- [x] 前述会话遗留：平台 MCP 模块 toolCount 为惰性填充（startServer 只拉起进程，点「查看工具」
  或 POST /{id}/test 才经 fetchTools 握手填充并写库）

## 当前状态
- MySQL: E:\KjlStudy\mysql:3306（root/racc123）；后端 8091 / 前端 8090 运行中
- MCP: mcp-tfs-requirements 已注册并 RUNNING（连接测试 success，4 工具）

## 注意事项
- 现成 data/mcp/tfs-query-winex（更全面的 TFS 工作项工具，含附件/建改）尚未注册进平台，需要的话可随时注册
- MCP 包 config.json 明文存 PAT；SVN/Git 若提交 data 目录需留意
- 前端「手动配置」创建弹窗已支持环境变量 + command 必填校验（上一会话完成）
- [x] 需求分析师闭环改造（诊断→修复）：
  - 诊断：Agent 挂 std-req-analysis-emr-v5（扫码版）但 system_prompt 是"不扫码"定位（应为 kb-only-v5）；
    E:\winning\workspace-ma 代码目录不存在 → emr-v5 流程卡死在代码扫描；mcp-tfs-requirements 只有读工具，
    缺回写/附件/建任务能力 → 流程走不完
  - 修复：① preferredSkills → std-req-analysis-kb-only-v5（与 system_prompt 一致，去代码扫描依赖）
    ② 注册 tfs-query-winex（id=3，RUNNING，连接测试 success，9 工具：get_work_item(s)/list_projects/
    list/download/upload 附件/update_work_item/create_work_item）
  - 待用户操作：Agent 修改弹窗勾选 MCP（tfs-query-winex 必选，mcp-tfs-requirements 可选）

================================================================================
SESSION: 2026-08-29 18:30
================================================================================

## 本次完成：沙箱管理从占位升级为真实可用的受控执行环境

### 背景
- 沙箱管理原为纯占位：listActive 恒空、销毁空方法、racc.sandbox.enabled 默认 false 且不在 yml；
  页面两处文案错误（提示 agentos.sandbox.enabled、"网络：none（无网络隔离）"自相矛盾）、
  引用不存在的 Dockerfile.sandbox。本机无 Docker，纯容器方案无法落地验证。

### 方案（用户确认：本地进程沙箱 + Docker 预留）
- 两级模型：沙箱实例（独立工作目录 data/sandbox/{id}）+ 执行记录（每条命令异步执行、输出落库）
- 引擎分层：LocalSandboxExecutor（真实，ProcessBuilder + cmd/sh -c）；
  DockerSandboxExecutor（预留，docker CLI 驱动，--memory/--cpus/--network none/--read-only，
  不可用自动降级 LOCAL，对齐平台降级家法）

### 改动清单
- 后端（均为 com.racc.sandbox 扁平包）：SandboxEntity/SandboxExecutionEntity（sandboxes、
  sandbox_executions 两表，ddl-auto 自动建）、两个 Repository、LocalSandboxExecutor、
  DockerSandboxExecutor、SandboxService 重写（有界线程池 1/2 + afterCommit 投递 +
  启动对账 + 审计写入 AuditTaskExecutionEntity taskType=SANDBOX，为审计表首个真实生产者）、
  SandboxController 重写（8 端点，旧契约 /status /active DELETE/{taskId} 全兼容）
- application.yml：racc.sandbox 块（enabled: true / dir / default-timeout-seconds 600 /
  max-output-chars 65536 / docker{enabled false, image, memory, cpus}）
- 前端：api/sandbox.ts 扩展（旧导出签名保留）、useStatusTag +DESTROYED、
  SandboxView.vue 重写（概览 4 卡 + 表格 + 创建/执行对话框 + 详情抽屉 + 输出对话框，
  抽屉 5s 轮询 silent+diff，UED 规范逐条落位）、根目录新建 Dockerfile.sandbox

### 验证（curl 冒烟全绿）
- status/创建/执行/输出落库 ✓；失败语义（exitCode=1 → FAILED）✓；
  3s 限制跑 ping -n 10 → TIMEOUT 强杀 ✓；重名 400/不存在 404/空命令 400/销毁后执行 400/重复销毁 400 ✓
- 中文编码坑：本机 Windows 命令输出混杂两种编码（cmd echo 走 GBK，ping 重定向输出 UTF-8），
  固定单一字符集必有一类乱码 → 改为字节缓存收尾解码（严格 UTF-8 优先，失败回退平台默认字符集），两类均验证通过
- 审计表 5 行落库 ✓；销毁后工作目录删除 ✓；旧契约 /active（taskId+status）✓
- 前端 vue-tsc：沙箱相关文件零错误（存量 4 个错误在 chat.ts/AgentConfigView/KnowledgeView，与本次无关）

### 已知限制
- LOCAL 模式与后端同权限，无硬隔离（页面文案如实说明）；后端重启不追杀孤儿子进程（超时/销毁兜底）；
  Git Bash 的 curl 传中文 JSON 会被控制台重编码（浏览器 axios 不受影响，测试用字节级 payload 规避）

### 待用户手动回归
- 页面操作流：创建→执行→抽屉看进度/输出→销毁；未启用告警可将 yml 改 false 重启验证

### 追加：开启/关闭做成运行时配置（同日 19:30）
- 诉求：沙箱启用状态不改文件、不重启即可切换
- 实现：复用平台既有 system_configs 运行时配置机制（TFS 同款）——
  SandboxService 读取顺序：system_configs(group=sandbox,key=enabled) 优先 → 缺省回退 yml；
  新增 POST /api/sandbox/config {enabled} upsert 该行；/status 增 enabledSource 字段
- 前端：概览首卡 el-tag → el-switch（乐观更新+失败回滚），副标签显示开关来源；未启用告警文案同步更新
- 验证全绿：开→关即时生效（创建接口 400 新文案）→ 开恢复；DB 行落库；非法入参 400；前端 HMR 无错
- 注意：一旦页面上切过开关，system_configs 行即存在并长期优先于 yml（yml 仅作初始默认值）

### 追加：沙箱关联任务下拉 + Docker 引擎运行时开关（同日 20:10）
- 创建沙箱「关联任务」下拉：数据源先改为自动化任务执行记录（/api/pipeline），
  后按用户要求再优化为自动化管理的任务类型（/api/automate/task-types，仅启用项，
  值=任务类型 code 如 req-consolidation，选项带图标+名称+code）；实体 javadoc 同步更新口径
- Docker 引擎开关也接运行时配置：system_configs(group=sandbox,key=docker.enabled)，
  DockerSandboxExecutor 注入 SystemConfigService，探测结果双重检查缓存 5 分钟（避免每次
  /status 都跑 docker CLI），切换开关即失效缓存；/status 增 dockerEnabledSource；
  POST /api/sandbox/config 扩展为 {enabled?, dockerEnabled?} 返回完整最新状态
- 前端：概览「启用状态」「Docker 引擎」两卡均为 el-switch（乐观更新+回滚），
  Docker 卡副标签显示 未启用/本机不可用/可用
- 验证全绿：开→关即时生效、来源标注正确（system_configs/application.yml）、DB 双行落库、
  非法入参 400；用户页面实测创建沙箱 req-analysis（关联任务类型）成功

### 追加：Docker Desktop 安装 + 容器模式全链路打通（同日 21:20）
- 预检：Win11(26200) + WSL2(Ubuntu) + 虚拟化已开 + C 盘 332G，条件齐全
- 安装：官方源 desktop.docker.com 实测 3.3MB/s 下载 602MB；Git Bash/cmd 直接跑安装程序
  返回 127（GUI 启动方式问题），改 PowerShell Start-Process -Wait 执行成功（EXIT=0）；
  Docker Engine CLI 29.7.2
- 拉镜像两个坑：① Docker Hub 官方源被墙（connectex 超时）→ 配 ~/.docker/daemon.json
  registry-mirrors（daocloud/1ms.run/dockerproxy.net）重启引擎；
  ② Git Bash 环境 docker-credential-desktop 不在 PATH → 把
  C:\Program Files\Docker\Docker\resources\bin 加进 PATH 后拉取成功
- 镜像构建：agentos-sandbox:latest 成功（debian:bookworm-slim + bash/curl/python3 + 非 root）
- 后端修复：DockerSandboxExecutor.create 挂载路径转绝对路径（DB 存相对路径，-v 必须绝对）；
  后端重启时 PATH 带上 Docker 目录（ProcessBuilder 找 docker 命令）
- E2E 全绿：平台 /status 显示 engine=DOCKER, dockerAvailable=true, imagePresent=true；
  创建 DOCKER 沙箱 → 真实容器 racc-sandbox-{id} Up；容器内执行命令返回
  uid=1000(sandbox)/Debian 12//work；只读根文件系统（写 / 被拒）、/work 写入宿主机
  data/sandbox/{id} 双向可见、外网访问被阻断（--network none）；销毁后容器移除 + 目录清空
- 当前状态：Docker 引擎开关已在 system_configs 置 true，沙箱默认引擎升级为 DOCKER；
  Docker Desktop 随桌面端常驻运行（后台任务），welcome-to-docker 欢迎容器已清理
- 注意：后端进程需 PATH 含 Docker 目录（本次会话已处理）；启动平台.bat 走系统 PATH，
  Docker 安装器已将其加入用户环境变量，新起进程不受影响

### 追加：左侧菜单整理（同日 21:30，已回退）
- 曾按「全量+高频置顶」把 26 页全部入菜单（6 分组+更多折叠）；用户反馈不希望展示
  原隐藏页面 → 已完整回退：恢复原 4 组 10 项（工作区/资产与能力/业务编排/连接与基座），
  16 个页面维持隐藏（URL 直达），App.vue 菜单/图标导入/systemSubMenuOpen 均还原
- 结论：菜单保持复刻参考平台的精简设计，隐藏页不主动开放

### 追加：Git 推送认证修复（同日 22:30）
- 现象：git push 报 Invalid username or token（GitHub 拒绝密码认证）
- 根因链：① ghfast.top 加速代理存的是 kjl+密码 格式旧凭据（第一代 GCMW 1.20.0 所存，
  GitHub 早已废弃密码认证）；② credential.helper=manager 指向的正是这个废弃的
  GCM for Windows 1.20.0（不支持 OAuth/设备码），本会话无交互终端时其 GUI 弹窗不可见，
  认证必挂
- 修复：删除 ghfast 旧凭据 → credential.helper 全局切到 manager-core（GCM 2.x）→
  origin 从 ghfast 代理切到 github.com 直连（实测可达）→ 最终发现本机已有
  ed25519 SSH 密钥且已注册在 GitHub 账号（493470065），直接改 SSH 远端：
  git@github.com:493470065/kjl-study.git，推送成功
- 现状：main 与 origin/main 同步；以后推送走 SSH，无需再输凭据、不会过期