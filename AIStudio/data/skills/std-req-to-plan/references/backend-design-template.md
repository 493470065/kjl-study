# 后端设计文档

## 一、设计概述

| 字段 | 内容 |
|------|------|
| 需求编号 | [工作项ID] |
| 技术栈 | Java / Spring Boot [版本] |
| 设计日期 | [日期] |

### 1.1 设计原则
- 遵循项目现有的分层架构（Controller → Service → DAO）
- 复用项目已有的工具类和公共组件
- 符合团队编码规范

## 二、API 端点设计

### 2.1 Controller 层

| 类名 | 路径 | 说明 |
|------|------|------|
| [XxxController] | /api/v1/xxx | [功能说明] |

#### [接口名称]

```
[GET/POST/PUT/DELETE] /api/v1/[resource]
```

| 项目 | 内容 |
|------|------|
| 请求参数 | [参数说明] |
| 响应数据 | [响应说明] |
| 权限要求 | [角色/权限] |
| 备注 | [特殊说明] |

## 三、Service 层设计

### 3.1 服务接口

```java
public interface [XxxService] {
    // 方法签名及说明
}
```

### 3.2 核心业务逻辑

#### [业务流程名称]

```
步骤1: [操作描述]
步骤2: [操作描述]
步骤3: [操作描述]
...
```

### 3.3 事务管理

| 方法 | 事务传播 | 说明 |
|------|---------|------|
| [methodName] | REQUIRED / REQUIRES_NEW | [说明] |

## 四、数据模型设计

### 4.1 新增/修改的 Entity

```java
// Entity 定义
@Entity
@Table(name = "[table_name]")
public class [EntityName] {
    // 字段定义
}
```

### 4.2 DTO / VO 定义

```java
// 数据传输对象
public class [DTOName] {
    // 字段定义
}

// 视图对象
public class [VOName] {
    // 字段定义
}
```

### 4.3 对象转换关系

```
Entity ←→ DTO ←→ VO
  ↑                    ↑
数据库              前端展示
```

[说明各层之间如何转换]

## 五、数据库设计

### 5.1 新增表

```sql
CREATE TABLE [table_name] (
    -- 字段定义
);
```

### 5.2 修改表

```sql
ALTER TABLE [table_name]
    ADD COLUMN [column_name] [type] [constraints];
```

### 5.3 索引设计

| 表名 | 索引名 | 字段 | 类型 | 说明 |
|------|--------|------|------|------|
| [table] | idx_[name] | [fields] | 普通/唯一 | [说明] |

## 六、中间件使用

### 6.1 缓存设计（如需要）

| 缓存Key | 数据 | TTL | 更新策略 |
|---------|------|-----|---------|
| [key_pattern] | [数据说明] | [过期时间] | [主动更新/被动过期] |

### 6.2 消息队列（如需要）

| Topic | 生产者 | 消费者 | 消息格式 | 说明 |
|-------|--------|--------|---------|------|
| [topic] | [服务] | [服务] | [格式] | [说明] |

## 七、DAO 层设计

### 7.1 Mapper / Repository

```java
public interface [XxxMapper] extends BaseMapper<[Entity]> {
    // 自定义查询方法
}
```

### 7.2 复杂查询

[描述需要手写的复杂 SQL，如多表关联、统计分析等]

## 八、异常处理

| 场景 | 异常类型 | 处理方式 | 返回码 |
|------|---------|---------|--------|
| [场景描述] | [异常类] | [处理逻辑] | [错误码] |

## 九、配置变更

### 9.1 应用配置

```yaml
# application.yml 新增配置项
[config-key]: [config-value]
```

### 9.2 环境差异

| 配置项 | 开发环境 | 测试环境 | 生产环境 |
|--------|---------|---------|---------|
| [key] | [dev-value] | [test-value] | [prod-value] |

## 十、注意事项

- [需要注意的并发问题]
- [需要注意的数据一致性问题]
- [需要注意的性能瓶颈]
- [需要和前端联调的接口]
