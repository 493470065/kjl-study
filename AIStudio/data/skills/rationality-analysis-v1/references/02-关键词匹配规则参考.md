# 关键词匹配规则参考（关键词匹配分类法）

以下为辅助录入模块的关键词匹配规则，可作为其他模块关键词设置的参考模版。

## 引用操作类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 引用操作-插入功能 | 插入、引用、导入、引用功能、导入选中 |
| 引用操作-勾选 | 勾选、复选、选中、勾选框、checkbox |
| 引用操作-批量 | 批量引用、批量插入、批量导入、批量操作 |
| 引用操作-快捷键 | 快捷键、快捷操作、热键、F键 |
| 引用操作-复制粘贴 | 复制、粘贴、copy、paste |
| 引用操作-引用流程 | 引用流程、引用步骤、操作步骤 |

## 性能类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 性能-加载优化 | 加载慢、卡顿、卡、性能、响应慢、优化、加载、耗时、延迟 |
| 性能-刷新问题 | 刷新、自动刷新、手动刷新、刷新机制 |

## 交互类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 交互-位置布局 | 位置、布局、弹窗位置、显示位置、窗口位置、侧边栏、右侧、底部弹出、面板位置 |
| 交互-按钮优化 | 按钮、操作按钮、功能按钮、右键菜单、导入选中 |
| 交互-弹框窗口 | 弹框、弹窗、窗口、全屏、拖动、拖拽、收缩、展开、最大化 |
| 交互-提示信息 | 提示、消息、提醒、通知、弹窗提示、alert |
| 交互-快捷操作 | 快捷、快速、一键、自动 |

## 显示类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 显示-字段列显示 | 字段、列、显示列、隐藏列、列宽、列配置、显示字段、隐藏字段 |
| 显示-格式显示 | 格式、显示格式、样式、字体、颜色、高亮、标记 |
| 显示-数据展示 | 展示、列表展示、数据展示、表格 |

## 排序类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 排序-数据排序 | 排序、顺序、倒序、升序、降序、时间排序、类型排序 |
| 排序-自定义排序 | 自定义排序、手动排序、排序配置 |

## 筛选类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 筛选-类型分类 | 筛选、过滤、分类、类型筛选、类别、检验类型、检查类型 |
| 筛选-时间范围 | 时间范围、日期范围、时间筛选、日期筛选、时间段 |
| 筛选-条件筛选 | 条件、筛选条件、查询条件、高级筛选 |

## 数据来源类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 数据来源-接口对接 | 接口、对接、数据源、来源、数据接口、API、第三方、外部数据 |
| 数据来源-数据同步 | 同步、更新、数据更新、实时同步 |

## 配置参数类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 配置参数-个人设置 | 个人设置、配置、参数、设置项、用户配置 |
| 配置参数-模板设置 | 模板、样式模板、格式模板 |
| 配置参数-系统配置 | 系统配置、全局配置、医院配置 |

## 权限控制类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 权限控制-角色权限 | 权限、角色、授权、可见权限、操作权限 |
| 权限控制-数据权限 | 数据权限、访问权限、查看权限 |

## 引用格式类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 引用格式-分隔符符号 | 分隔符、符号、标点、冒号、逗号、句号、分隔 |
| 引用格式-格式模板 | 格式模板、输出格式、插入格式 |

## 功能类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 功能-特殊符号 | 符号、公式符号、节气、医学符号、特殊符号 |
| 功能-术语申请 | 术语、术语申请、术语库、短语 |
| 功能-影像调阅 | 影像、调阅、图片、趋势图、图形 |
| 功能-数据导出 | 导出、打印、输出 |

## 状态类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| 状态-勾选状态 | 勾选状态、状态管理、状态同步 |
| 状态-引用状态 | 引用状态、已引用、未引用 |

## BUG类
| 问题点 | 关键词匹配规则 |
|--------|----------------|
| BUG-异常报错 | 报错、异常、错误、失败、BUG、bug、崩溃、无法 |