# 后端设计文档

## 一、设计概述

| 字段 | 内容 |
|------|------|
| 需求编号 | 1455978 |
| 技术栈 | Java 8 / Spring Boot |
| 设计日期 | 2026-04-15 |

### 1.1 设计原则
- 遵循项目现有的分层架构（Controller → Service → DAO）
- 复用项目已有的工具类和公共组件
- 符合团队编码规范
- 最小化对现有代码的修改

## 二、API 端点设计

### 2.1 急诊模块 Controller 层

| 类名 | 路径 | 说明 |
|------|------|------|
| EmgEmrStatusController | /api/v1/emg/emr/status | 急诊病历状态管理 |

### 2.2 门诊模块 Controller 层

| 类名 | 路径 | 说明 |
|------|------|------|
| OptEmrEmgStatusController | /api/v1/opt/emr/emg-status | 接收急诊病历状态 |

## 三、Service 层设计

### 3.1 急诊模块服务接口

```java
public interface EmgEmrStatusNotifyService {
    EmgEmrStatusNotifyResponse notifyStatusChange(EmgEmrStatusNotifyRequest request);
    boolean retryNotify(String syncId);
}
```

### 3.2 门诊模块服务接口

```java
public interface OptEmrEmgStatusReceiveService {
    EmgEmrStatusReceiveResponse receiveEmgStatus(EmgEmrStatusReceiveRequest request);
    List<OptEmrEmgStatusDTO> queryEmgStatus(String patientId, String encounterId);
}
```

## 四、数据模型设计

### 4.1 Entity 定义

#### EmgEmrStatusSyncLogPO - 急诊状态同步日志表

```java
@Entity
@Table(name = "emg_emr_status_sync_log")
public class EmgEmrStatusSyncLogPO {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String syncId;
    private String emrSetId;
    private String patientId;
    private String encounterId;
    private String status;
    private String syncStatus;
    private Integer retryCount;
    private String errorMessage;
    private Date createdTime;
    private Date updatedTime;
}
```

## 五、数据库设计

### 5.1 新增表

```sql
CREATE TABLE emg_emr_status_sync_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    sync_id VARCHAR(64) NOT NULL,
    emr_set_id VARCHAR(64) NOT NULL,
    patient_id VARCHAR(64) NOT NULL,
    encounter_id VARCHAR(64) NOT NULL,
    status VARCHAR(32) NOT NULL,
    sync_status VARCHAR(16) NOT NULL,
    retry_count INT NOT NULL DEFAULT 0,
    error_message VARCHAR(512),
    created_time DATETIME NOT NULL,
    updated_time DATETIME,
    INDEX idx_sync_id (sync_id),
    INDEX idx_sync_status (sync_status)
) COMMENT = '急诊状态同步日志表';
```

```sql
CREATE TABLE opt_emr_emg_status (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    record_id VARCHAR(64) NOT NULL,
    emr_set_id VARCHAR(64) NOT NULL,
    patient_id VARCHAR(64) NOT NULL,
    encounter_id VARCHAR(64) NOT NULL,
    emr_type VARCHAR(16) NOT NULL,
    status VARCHAR(32) NOT NULL,
    change_time DATETIME NOT NULL,
    operator_name VARCHAR(128),
    dept_name VARCHAR(256),
    created_time DATETIME NOT NULL,
    UNIQUE KEY uk_record_id (record_id),
    INDEX idx_patient_encounter (patient_id, encounter_id)
) COMMENT = '门诊急诊病历状态表';
```

## 六、DAO 层设计

### 6.1 Repository

```java
public interface EmgEmrStatusSyncLogJpaRepo extends JpaRepository<EmgEmrStatusSyncLogPO, Long> {
    List<EmgEmrStatusSyncLogPO> findBySyncStatusAndRetryCountLessThan(String syncStatus, int maxRetryCount);
}

public interface OptEmrEmgStatusJpaRepo extends JpaRepository<OptEmrEmgStatusPO, Long> {
    Optional<OptEmrEmgStatusPO> findByEmrSetId(String emrSetId);
    List<OptEmrEmgStatusPO> findByPatientIdAndEncounterIdOrderByChangeTimeDesc(String patientId, String encounterId);
}
```

## 七、配置变更

```yaml
emr:
  emg:
    status-sync:
      enabled: true
      endpoint: http://localhost:8080/api/v1/opt/emr/emg-status/receive
      retry:
        max-attempts: 3
```

## 八、注意事项

1. 并发问题: 同一病历的状态变更可能并发，需要使用分布式锁保证顺序
2. 数据一致性: 急诊和门诊的状态可能不一致，需要实现对账机制
3. 性能问题: 状态变更频繁时，考虑批量处理
4. 监控告警: 需要监控同步成功率
