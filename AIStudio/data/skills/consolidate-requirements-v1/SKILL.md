---
name: consolidate-requirements-v1
entry: scripts/consolidate.js
description: 从 TFS 查询获取需求清单，按 Spec 知识库功能点结构聚拢输出，并输出功能点健康度报告（含五维评分框架+一票否决制的设计合理性评估）。当用户说"聚拢需求"、"需求按Spec归类"、"按知识库聚拢需求"、"TFS需求清单"、"需求按模块分类"、"健康度报告"、"功能点健康度"、"设计合理性"、"五维评分"、"一票否决"时使用。也适用于需要将 TFS 工作项映射到知识库功能模块树的场景。默认查询：病历条线新增需求；默认输出路径：E:\37结构性问题治理\07病历条线需求聚拢\
---

# consolidate-requirements-v1

将 TFS 查询中的工作项（包括**需求**和**软质**），按照 Spec 知识库的功能模块结构树进行聚拢分类，输出结构化的需求清单（Markdown 文件）。

- **需求** = 功能性 + 接口
- **软质** = 软件质量（非功能性）

## 🚀 快速使用

### 方式1：聚拢需求清单
```bash
node ~/.claude/skills/consolidate-requirements-v1/scripts/consolidate.js
```

### 方式2：将 MD 报告转为 HTML（含导航树）
```bash
node ~/.claude/skills/consolidate-requirements-v1/scripts/generate-html-report-v3.js
```

输出文件自动生成到：`E:\37结构性问题治理\07病历条线需求聚拢\`

### 方式3：JSON 模式（供 AIStudio 需求归集「技能数据源」消费）
```bash
node scripts/consolidate.js --json          # 或调用参数 {"json": true}
```
stdout 仅输出功能点 FPI JSON 数组：`[{code,name,module,total,req,soft,avgMonthly,softRatio,trend,fpi}]`，进度信息走 stderr。

支持参数（写在调用参数 JSON 里）：`line`（inpatient/住院、outpatient/门诊、emergency/急诊，只在该条线功能点池中匹配并输出）、`queryUrl`（覆盖默认 TFS 查询）、`since`（历史生命周期模式，格式 YYYY-MM-DD，见方式5）、`ids`（工单 ID 数组，见方式6）。

### 方式4：MD 报告模式（供 AIStudio 需求归集「导出 MD」下载）
```bash
node scripts/consolidate.js --md            # 或调用参数 {"md": true, "line": "..."}
```
执行完整聚拢流程，stdout 仅输出 MD 报告全文（进度走 stderr，不写文件）。与 JSON 模式共用 `line`/`queryUrl` 参数；两者同时传时 JSON 优先。

### 方式5：历史生命周期模式（健康度规则 v2.0，推荐用于健康度分析）
```bash
node scripts/consolidate.js --json --since=2025-01-01   # 或 {"json": true, "since": "2025-01-01"}
```
不直接执行存储查询（它只是"新增需求"近期切片），而是按数据口径自建 WIQL，拉取 **CreatedDate >= 指定日期的全量需求+软质**（横跨 WiNEX-Inpatient-2 / WiNEX-Outpatient / WiNEX-Emergency 三个项目，实测 2025-01-01 起约 1.9 万项，运行约 1 分钟）。其余管道（基线过滤/语义匹配/聚拢）全部复用，JSON 输出增加 `recurrence`（复发对数）、`quarterly`（季度分桶）、`firstDate`/`lastDate`/`spanMonths` 字段，FPI 启用 **v2 公式**（新增复发惩罚 ≤15 + 长尾惩罚 5）。健康度判定规则见 `references/design-rationality-rules.md` **v2.0**（量化 HPI 与五维评审双轨融合 + 一票否决 V1-V6）。

> ✅ **2026-09-07 起页面"技能数据源"数据口径**：前端在归集时把该条线**数据源链接拉取到的「剩余需求+剩余软质」工单 ID 去重合集**注入 `ids` 参数（方式6），FPI 严格按链接拉取的未完结数据统计（剩余需求=需求类且状态∈已建议/活动/已分析；剩余软质=软质类且状态非完结态，口径与归集统计表一致）；链接未拉取到数据时回退注入 `{"json":true,"since":"2025-01-01"}` 全量口径。用户显式传 ids/since/md/queryUrl 时不注入。`executeSkill` 传 `timeoutMs:180000` 且 axios 超时随其后（原全局 30s 会先断），后端 stdout 截断上限已从 2MB 放宽至 8MB。ids 模式实测 <1s，历史口径端到端约 64s。

### 方式6：链接工单集合模式（`ids` 参数，需求归集页面 FPI 默认口径）
```bash
node scripts/consolidate.js --json --ids=1750924,1750925   # 或 {"json": true, "ids": [1750924, ...]}
```
对外部传入的工单 ID 集合（去重）直接批量拉详情并走完整聚拢管道（基线过滤/语义匹配/功能点 FPI），**跳过 WIQL 查询**——即"按数据源链接拉取的剩余需求+剩余软质进行统计"（ID 集合由前端按剩余口径过滤后传入，技能原样消费）。优先级高于 `since`；启用 v2 健康度公式（复发/长尾惩罚）；JSON 顶层 `window.mode='link-ids'`、`linkIds` 为传入数量。实测 187 项 794ms 完成，适合页面实时调用。

## 工作流程

### 步骤0：获取 Spec 知识库（动态加载）

脚本启动时自动从 Git 仓库获取最新 Spec 知识库：
```
http://tfs2018-web.../winning-record-konwledge/Spec/
```

获取 3 份功能结构树文件，解析出 33 个模块、214 个功能点的定义。如果 Git 仓库不可用，自动回退到本地缓存。

### 步骤1：获取输入（使用默认值）

脚本已内置默认值，可直接运行：

- **TFS 查询 URL** → 病历条线新增需求查询（已内置）
- **PAT 令牌** → 已配置
- **输出路径** → 默认 `E:\37结构性问题治理\07病历条线需求聚拢\`，自动按当天日期命名

如需切换到其他查询，仍可传入参数覆盖默认值。

### 步骤2：检查依赖

确保 Node.js 可用（该脚本使用 Node.js 内置 http 模块，无需 npm 安装）：

```bash
node --version
```

### 步骤4：执行聚拢脚本

```bash
node <skill-path>/scripts/consolidate.js [tfs-query-url] [pat-token] [output-path]
```

**三种使用方式**：

```bash
# 方式1：无参数运行（使用默认查询、令牌、路径）
node ~/.claude/skills/consolidate-requirements-v1/scripts/consolidate.js

# 方式2：仅指定输出路径（使用默认查询和令牌）
node ~/.claude/skills/consolidate-requirements-v1/scripts/consolidate.js \
  "" "" "E:\37结构性问题治理\07病历条线需求聚拢\病历新增需求归集-自定义.md"

# 方式3：全部自定义（覆盖默认值）
node ~/.claude/skills/consolidate-requirements-v1/scripts/consolidate.js \
  "http://tfs:8080/tfs/Collection/Project/_queries?id=xxx&_a=query" \
  "your-pat-token" \
  "E:\37结构性问题治理\07病历条线需求聚拢\病历新增需求归集-20260819.md"
```

### 步骤5：匹配流程（五阶段漏斗）

```
获取需求 → 先基线判断 → 基线能力 → 标记基线排除 ✔
                      → 应纳入功能点 → 语义/关键词匹配 → 归入现有功能点 ✔
                                        → Spec 知识库匹配（FTS 分词+投票）→ 归入现有功能点 ✔（confidence=spec-kb）
                                        → 无匹配
                                            ├─ 软质项 → 归入模块级软质统计 ✔
                                            └─ 非软质 → 建议新增功能点 ⭐
```

#### 阶段1：方法论基线过滤（优先）

先应用 [WiNEX 功能点划分原则与方法论](references/WiNEX功能点划分原则与方法论.md) 判断每个需求是否属于基线能力：

| 基线类型 | 示例 | 处理方式 |
|:---------|:-----|:---------|
| 单对象增删改查 | 新增患者、删除医嘱、修改药品信息 | ❌ 排除 |
| 列表/导出/打印 | 导出费用清单、打印腕带、查询检验报告列表 | ❌ 排除 |
| 字典/配置管理 | 维护药品字典、配置角色权限 | ❌ 排除 |
| 纯技术接口 | REST API、HL7 消息通道、数据库同步 | ❌ 排除 |
| ✅ 有业务交互特征 | 涉及校验/审核/审批/触发/联动/自动生成/预警等 | ⭐ 继续匹配 |

#### 阶段2：语义匹配（LLM 映射表）

通过基线的需求，先查 AI 语义映射表（`semantic-fp-map.json`，需求 ID → 功能点精确映射）；未命中再按精标关键词库（`fp-map.js`）打分匹配（功能点名称出现 +3、关键词命中、TFS 模块归属加分，阈值 ≥2 分）。

#### 阶段3：文书 Spec 知识库匹配（2026-09-07 新增）

关键词未命中时，把工单标题**按领域词表分词**（Spec 功能点名称 2-gram + 业务特征词，滤除噪音），调 AIStudio 知识库 FTS 检索（`GET /api/knowledge/search?q=词1 词2...&topK=6&productLine=...`，productLine 按条线映射 BLGL→inpatient-emr / MZBL→outpatient-emr / JZBL→emergency-emr），对 topK 命中结果**按功能点投票**——同一功能点的多篇 Spec 文档（-Spec / -Analyst-spec / -PM-spec）命中 ≥2 票才采纳，归入该功能点并标记 `confidence='spec-kb'`。只采纳 Spec 树内功能点；默认启用（`{"specKb": false}` 关闭，`kbApi` 可指定平台地址）；每标题有缓存，查询上限 400 次/运行，平台不可达时自动降级跳过。适用于新需求词库未覆盖的场景；JSON `window.kb` 输出查询/命中统计。

#### 阶段4：软质项兜底

语义匹配不到的软质项（`requirementType = '软件质量'`），按模块级归类，归入软质统计。连模块都匹配不到的软质项，归入基线排除。

#### 阶段5：模块级匹配（兜底）

语义匹配不到的非软质需求，先按 TFS 模块名称映射，再按关键词匹配到模块级。如果仍匹配不到，标记为"建议新增功能点"。

### 步骤6：输出结果

脚本输出为 Markdown 文件，每个子系统包含以下章节（主章节六章 + 文末独立章节）：

#### （一）归集汇总
```
| 模块名称 | 匹配功能点数 | 问题数 | 合并需求 | 独立需求 | 软质数 | 占比 |
|:---------|:-----------:|:-----:|:--------:|:--------:|:-----:|:----:|
| BLGL-01-BLSX 01 病历书写 | 10 | 31 | 0 | 28 | 3 | 25% |
| **合计** | **48** | **126** | **0** | **83** | **43** | **100%** |
```

> 问题数 = 合并需求 + 独立需求 + 软质数

#### （二）合并需求详情
列出所有合并需求项的 ID、标题、所属模块、类型。

#### （三）软质详情
列出所有软质问题的 ID、标题、所属模块。

> 注：原「基线需求详情」章节已移除（未匹配到具体功能点的模块级需求归入基线排除清单）。基线排除清单移至文末独立章节并带合计统计。

#### （四）功能点需求详情
```
### BLGL-01-BLSX 01 病历书写

| Spec 功能点 | 状态 | 类型 | ID | 标题 |
|:-----------|:----:|:----:|:--:|:------|
| BLGL-01-BLSX-002 病历编辑与保存 | ● 活动 | 功能性的 | #1683944 | 入院记录... |
```

> 仅展示 `requirementType = '功能性的'` 的需求，不展示软质和接口模块（BLGL-98-JK）。

#### （五）建议新增功能点
列出应纳入功能点但未匹配到现有 Spec 功能点的需求。

#### （六）健康度分析
```
| 功能点 | 问题数 | 健康等级 | 评估 |
|:-----|:-----:|:--------:|:-----|
| BLGL-10-BLCX-003 病历结构化查询 | 14 | 🔴 危险 | 新增需求过载，需考虑重构 |
```

> **v2.0 起健康度判定为双轨融合**：量化 HPI（脚本 `--json` 输出的 `fpi` 字段，历史模式含复发/长尾惩罚）× 五维评审（`references/design-rationality-rules.md` v2.0），最终等级查融合矩阵；一票否决扩展为 V1-V6（新增 V6 复发性设计缺陷）。仅凭 HPI/问题数直接定级是不完整的——③法规合规、④竞品差距必须独立评审。

## 聚拢规则

聚拢采用**四阶段漏斗策略**：

### 阶段1：方法论基线过滤（优先）

先判断每个需求是否应纳入功能点。基线能力（单对象增删改查/列表导出打印/字典配置/纯技术接口）直接排除，不纳入功能点统计。

### 阶段2：语义匹配（LLM 映射表）

通过基线的需求，查询 AI 语义映射表 `references/semantic-fp-map.json`，匹配到具体的 Spec 功能点。该映射表由 AI 根据需求标题+描述语义判断，无需维护关键词库。

### 阶段3：软质项兜底

语义匹配不到的软质项（`requirementType = '软件质量'`），按模块级归类，归入软质统计。连模块都匹配不到的归入基线排除。

### 阶段4：模块级匹配（兜底）

语义匹配不到的非软质需求，先按 TFS 模块名称映射，再按关键词匹配到模块级。如果仍匹配不到，标记为"建议新增功能点"。

## 输出文件结构

```
~/.claude/skills/consolidate-requirements-v1/
├── SKILL.md                    ← 技能说明
├── scripts/
│   └── consolidate.js          ← 主脚本（Node.js）
└── references/
    ├── knowledge-base.js       ← 知识库：Spec 模块定义、映射表、关键词规则
    ├── external-rules.js       ← 外部规则：基线过滤规则
    ├── fp-map.js               ← 214 个功能点关键词映射表（供参考）
    ├── semantic-fp-map.json    ← AI 语义匹配映射表（由 LLM 生成，替代关键词匹配）
    ├── generate-report.js       ← 报告生成器
    ├── output-template.md      ← 输出格式模板（排版固化）
    ├── design-rationality-rules.md ← 设计合理性判定规则（五维评分框架 + 一票否决制）
    └── WiNEX功能点划分原则与方法论.md  ← 方法论文档（基线过滤依据）
```

## 数据口径说明

> 用于审计和追溯数据筛选逻辑。

### 聚拢范围
- **产品名** ∈ {WiNEX 病历管理, 03 WiNEX 急诊病历, WiNEX 门诊病历管理}
- **AreaPath 排除**：`WiNEX-Inpatient-2\病历质控` 与 `WiNEX-Outpatient\病历质控`
- **工作项类型**：同时纳入**需求**（功能性 + 接口）和**软质**（软件质量），不做类型过滤

### R27 过滤
- `ResolvedDate = ''` OR `ResolvedDate ≠ ClosedDate`

### 历史生命周期模式（v2.0，`--since` 启用）的数据口径
- 自建 WIQL，**不经过存储查询**：`CreatedDate >= since` + 产品名 IN（三产品）+ 类型 IN（需求/软质）+ AreaPath 排除两个质控区域
- R27 过滤在 WIQL 中带上；字段在部分类型上不存在导致查询失败时自动降级去掉
- 实测规模：2025-01-01 起约 1.9 万项，批量详情拉取约 95 批，整体运行约 1 分钟
- FPI v2 公式：`100 − 软质比惩罚(≤50) − 升温惩罚(≤20) − 复发惩罚(每对扣3, ≤15) − 长尾惩罚(跨度≥18个月扣5)`；工单数 < 3 时全部惩罚按样本量缩放
- 复发检测：同功能点内标题分词（CJK bigram + 英文词）Jaccard ≥ 0.45 且时间差 > 90 天记一对复发

### 需求分类
| 类别 | 定义 |
|:-----|:------|
| **需求** | 功能性 + 接口 |
| **软质** | 软件质量（非功能性） |

### 来源分类
| 类别 | 判定规则 |
|:-----|:---------|
| **医院需求** | `CreatedBy` 含 `TfsInterface` |
| **内部优化** | 其他创建者 |

### 合并需求识别
正则：`/合并\d{3,}|@1?\d{6,}|（合并|合并多|合并代码|合代码|合并需求\d|合并需求至|合并需求：|合并【\d+】|克隆主数据|历史需求合并|合并升级|合并单|合并到版本|合并至版本|至\d{6}迭代|到\d{6}迭代|至\d{6}版本|至泰康\d/`（覆盖「合并+单号/版本号」自由格式，含医院创建的合并升级）

### 主题分类
- **标题正则（细粒度）优先**：按标题正则表达式匹配进行主题分类
- **标题归 G 时 TFS Module 标准模块兜底**：当标题无法匹配时，使用 TFS 模块名称字段

### 本次未使用主题关键词筛选
- 仅按产品过滤
- 主题分类由 AI 在主 session 内动态归纳

## 注意事项

- **默认令牌**已内置在脚本中，直接运行即可
- **默认查询**为病历条线新增需求查询（WiNEX 病历管理/急诊病历/门诊病历管理）
- 如需切换其他查询，传入参数覆盖默认值即可
- PAT 令牌需要 **Work Items 读取** 权限
- 脚本默认使用 Basic Auth 认证（格式: `PAT:{token}`）
- TFS 2018 兼容，使用 `api-version=2.0`
- 单次查询最多支持 20000 个工作项，超过需分批处理
- **语义映射表** `semantic-fp-map.json` 由 AI 维护，当新增需求或功能点变化时需同步更新