---
name: std-req-analysis-ma
description: Use when用户提供 TFS 需求号，要求对医疗信息系统需求做正式详细需求分析、产品业务分析、客户需求分析、分析需求、需求分析、生成/回写产品业务分析.md、写入 Winning.Demand.Analysis 或追加 AI-ANALYSIS。适用于病案、病历等医疗产品线 TFS 需求；必须有 TFS 需求号。不用于预分析、批量筛选、Bug 修复、代码开发、实施计划、前后端设计文档、API/数据库设计或无 TFS 需求号的离线分析。
---

# 医疗信息系统正式详细需求分析

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.3.0 | 2026-05-28 | 1. Section 3 复杂度判定从二级（简单/复杂）改为三级（快速/标准/复杂），新增信号判定表格和模式影响表格；新增"复杂需求"标记短路规则；全文引用统一为"快速模式/标准模式/复杂模式"。2. 需求分析模板新增 🔴 标记突出重点章节（2.1 功能新增或修改、2.2 原型设计、2.3 参数配置）。3. 正式交付标签新增 EMR-AI-ANALYSIS-V2（保留原有 AI-ANALYSIS） |
| v1.2.0 | — | R10 autoplan 待确认项先澄清规则；R11 深度评审限范围规则；R12 附件版本治理规则 |
| v1.1.0 | — | 初始版本，二级复杂度（简单/复杂） |

把 TFS 医疗信息系统需求转成可确认、可追溯、可回写 `Winning.Demand.Analysis` 的正式产品业务分析正文。分析阶段不修改代码、不创建开发分支、不拆研发计划；代码、功能清单、参数和知识资料只用于验证现状、判断边界、形成产品设计、原型字段明细和测试用例。

## 0. 适用边界

- **必须有 TFS 需求号**：无 TFS 需求号即停止，不生成 `产品业务分析.md`，不搜索代码，不读取旧文件，不回写 TFS。
- **输入来源独立**：只以当前 TFS 工作项、附件/图片、前置/相关工作项、功能清单、参数和代码现状为输入；不依赖其他技能、历史草稿或本地过程文件。
- **只做产品业务分析**：不生成研发实现方案、接口设计、数据库设计、开发任务拆分或研发计划。
- **不处理时限讨论**：不追加“时限要求/AI时限建议”类讨论。
- **正式交付固定三件事**：写入 `Winning.Demand.Analysis`，追加 `AI-ANALYSIS` 和 `EMR-AI-ANALYSIS-V2`，上传最终版 `产品业务分析.md`。
- **自动创建分析任务**：正式分析完成后默认检查并创建 `需求分析_<需求号>` 子任务，已存在则跳过。

## 1. 强制规则

| 编号 | 规则 |
| --- | --- |
| R1 | TFS MCP 不可用即停止：明确提示当前会话 TFS MCP 未配置或不可用，不改用 TFS Web、未认证脚本、旧文件或用户临时材料继续分析。 |
| R2 | 同一需求全程使用同一 TFS 集合/`collectionHint`：读取、附件下载、图片处理、正文回写、标签追加、附件上传和任务创建必须一致。 |
| R3 | 产品线只根据 `Winning.Product.Name` 判断：病案用 `medicalArchive`，病历用 `medicalRecord`；不得用标题、描述或区域路径推断。 |
| R4 | 有图必处理：描述或相关工作项中出现 `<img>`、图片、附件、`FileNameGuid`、`_apis/wit/attachments` 时，必须下载或保留线索，并在正文生成前读取图片内容；未读图不得写“已处理图片”。 |
| R5 | 先查现状再定方案：必须搜索本次产品线对应的功能清单、参数和前后端代码，验证现有行为、相似功能、参数配置、字段来源和旧逻辑；代码搜索必须限关键词、限目录、限结果数，禁止无边界全仓库反复扫描。 |
| R6 | 正文纯净：`Winning.Demand.Analysis` 和最终汇报只写业务结论、功能设计、原型字段变更明细、必要影响范围、待确认问题和测试用例；工具日志、代码路径清单、下载过程、乱码排查、接口报错只写入 `notes.md`。 |
| R7 | 源稿回写：默认以 UTF-8 `产品业务分析.md` 作为唯一源稿，通过 TFS MCP 文件路径回写能力转换 HTML 并写入；每次修改 Markdown 后，必须重新从该 Markdown 回写 `Winning.Demand.Analysis`，不得只更新附件或使用脚本中旧的硬编码 HTML。 |
| R8 | 保持兼容旧行为：不得为单个医院直接破坏其他医院、院区、角色、旧版本、历史数据或存量配置；冲突场景优先参数、配置、字典、权限或新增独立入口。 |
| R9 | 待确认项不能吞并：只要正文出现“需确认、待确认、由研发确认、由产品确认、具体字段/口径/落点不明确、无法确定”等未决表述，必须输出“待确认问题”章节或改成确定方案。 |
| R10 | autoplan 待确认项先澄清：复杂/高风险需求使用 `gstack/autoplan` 深度评审时，如评审发现影响方案、边界、字段口径或测试用例判断的确认项，必须像 `brainstorming` 一样逐项向用户澄清；用户确认后把结论并入正文再回写。 |
| R11 | 深度评审限范围：`gstack/autoplan` 只评审产品业务分析草案、需求原文摘要、关键附件结论和代码定位摘要，不重新做全仓库代码探索、不重新下载附件、不生成研发计划，默认最多一轮。 |
| R12 | 附件版本治理：同一轮最终版 `产品业务分析.md` 只保留一份；评审后只做回写同步、排版修正、表头/格式微调或未证实的修订时，不得再次上传附件。确有业务内容变更需要更新附件时，优先覆盖同名附件，并在附件注释中写清真实业务变更原因。 |

## 2. 执行流程

1. **识别需求号和前置条件**：校验 TFS 需求号、TFS MCP、代码路径配置。
2. **读取工作项（第一波并行）**：

> ⚡ **并行优化**：TFS 读取均为独立网络调用，分两波并行执行以缩短 I/O 等待时间。

**第一波并行（互不依赖，同时发起）**：
1. `get_work_item(需求号)` — 获取标题、描述、产品名称、状态、优先级、标签、客户/医院、版本、需求分析字段、集合来源
2. `list_attachments(需求号)` — 获取附件列表（与 #1 无依赖）
3. 对每个前置/参见需求、父子项、关联 Bug，同时发起 `get_work_item(关联ID)`
4. 保存原始描述快照到 `<需求号>/<yyyyMMdd-HHmmss>/source.html`（本地写操作，可与 TFS 调用并行）
3. **创建运行目录**：使用当前工作目录下 `<需求号>/<yyyyMMdd-HHmmss>/`；保存 `source.html` 或 `source.md`。
4. **处理附件和图片（第二波并行）**：

**第二波并行（收到附件列表后）**：
1. 对所有附件同时发起 `download_attachments`，批量下载到 `attachments/`
2. 对 `System.Description` 中提取的内嵌图片（`<img src="...">`、`FileNameGuid`、`_apis/wit/attachments`），并行下载

> ⚠️ **强制要求**：若需求说明字段（`System.Description`）内容存在"附件"或"图片"，**必须下载并分析解读**！

附件和内嵌图片处理要点：
- 即使 `list_attachments()` 返回 0，也不能直接判断没有图片；还要继续从 `System.Description` 中提取 `<img src="...">`、附件链接、`FileNameGuid` 等可点击图片地址
- 内嵌图片要通过已认证的 TFS MCP 或封装脚本下载，优先复用同一份配置里的 `serverUrl + PAT`
- 若图片 URL 访问后返回 401/403，先判断是权限问题还是链接失效，再决定是否写入 `notes.md`
- 图片、截图、原型图和流程图都要作为需求输入解读；如果无法识别内容或下载失败，要把文件名、原因和影响记录到 `notes.md`
- 解读界面图时至少提取入口位置、区域名称、节点/字段/按钮、权限或角色差异、默认值/空态、跳转或保存动作；看不清的内容写成待确认问题
5. **读取相关工作项**：只读取当前需求明确提到、TFS 关系直接关联且与本次诉求相关的前置、参见、父子项或相关 Bug；不要遍历全部历史需求、全部子项或泛化相似需求。其中的图片/附件仅在影响本需求理解时处理。
6. **选择产品线和代码目录**：按 `Winning.Product.Name` 读取 `config/codebase-paths.json`。
7. **判断复杂度**：按第 3 节三级复杂度判定规则判断（快速/标准/复杂）。
8. **复杂需求澄清**：复杂需求先进入 brainstorming 和人工确认；确认前不生成正式正文、不回写 TFS、不上传临时澄清附件。用户确认后继续后续正式流程，后续第 12 步的 `gstack/autoplan` 深度评审仍必须执行。
9. **搜索现状**：按第 4.1 节定位策略执行。先检查 `graphify-out/graph.json`，标准/复杂模式按策略A（Graphify 并行查询）+ 策略B（并行 grep 搜索）执行；快速模式直接按策略B执行。全程在 `notes.md` 记录检查结果和查询过程。
10. **按需查参数和知识资料**：仅在需求涉及参数、配置、院区差异、兼容旧行为、字段/口径或现有能力复用时读取。
11. **生成正式正文草案**：使用 `assets/product-business-analysis-template.md`。正文必须突出三项核心内容：🔴功能新增或修改（2.1）、🔴原型设计及字段变更明细（2.2）、🔴参数配置（2.3）。
12. **质量检查**：快速模式轻量自检；标准模式自适应（轻量/深度）；复杂模式必须使用 gstack/autoplan 深度评审，不可用或预计耗时过高时回退增强自检。若 autoplan 深评发现待确认项，先逐项人工澄清，确认后更新 Markdown 并重新回写。
13. **按需原型设计**：只有新增界面、重大布局修改、配置界面或大交互变更且用户确认需要原型时触发；原型必须用户确认后才上传 TFS 附件，并在正文中记录。普通字段、按钮、列表列、文案和导出调整不做原型。
14. **回写正式正文**：调用 `update_demand_analysis(id, markdownFilePath, collectionHint)` 或等价 MCP 文件路径能力写入 `Winning.Demand.Analysis`。
15. **追加标签和上传源稿**：追加 `AI-ANALYSIS` 和 `EMR-AI-ANALYSIS-V2`；仅在本轮尚未上传最终版或存在已确认的业务内容变更时，上传/覆盖最终版 `产品业务分析.md`，不得因表头、排版或回写同步单独新增一份附件。
16. **自动创建分析任务**：按 `scripts/create-analysis-task.md` 检查并创建 `需求分析_<需求号>` 子任务，已存在则跳过。
17. **复核收口**：重新读取工作项一次，确认正文、标签、附件和任务创建结果。

## 3. 复杂度判定和模式分流

在完成需求输入和代码现状读取后，必须先判定复杂度，决定后续执行深度。

**短路规则**：如果需求标题、描述或 `System.Tags` 中包含"复杂需求"标记，**直接判定为复杂模式**，跳过下表其他信号判定。

| 信号 | 快速模式（全部不满足） | 标准模式（满足任一） | 复杂模式（满足任一） |
|------|----------------------|---------------------|---------------------|
| **标记"复杂需求"** | 无标记 | 无标记 | **需求标题/描述/标签中明确标记"复杂需求"** |
| 涉及模块数 | 单模块 | 2+ 模块 | 3+ 模块或跨系统依赖 |
| 需求描述长度 | < 500 字，描述明确 | ≥ 500 字或描述模糊 | ≥ 500 字 + 多附件/图片 |
| 业务角色 | 单一角色 | 多角色、多场景 | 多角色 + 流程配置/画布/拖拽 |
| 前置依赖 | 无前置需求 | 有前置/参见需求 | 前置 + 父子项 + 关联Bug |
| 数据库变更 | 无 | 无 | 新增表/字段/配置表 |
| 原型需求 | 无 | 简单界面调整 | 新增界面/重大布局修改 |
| **会诊计费流程** | 不涉及 | 不涉及 | **涉及会诊模块的计费/收费流程修改** |
| **病历审签流程** | 不涉及 | 不涉及 | **涉及病历审签流程修改（审批节点、签名规则、流转逻辑）** |

**模式影响**：

| 阶段 | 快速模式 | 标准模式 | 复杂模式 |
|------|---------|---------|---------|
| 代码定位 | 仅 grep 关键词搜索 | graphify(强制检查) + grep + 参数库 | graphify(强制检查) + grep + 参数库 + 深度追溯 |
| Brainstorming | 仅业务意图 | 业务意图 + 边界条件 | 业务意图 + 边界条件 + 代码发现追问 |
| 质量检查 | 轻量自检 | 自适应（轻量/深度） | 深度评审 |
| 参数查询 | 不查 | 按需查 | 必查 |
| 原型设计 | 不设计 | 按需设计 | 必须设计 |
| 预计时间 | 5-10 分钟 | 10-20 分钟 | 20-30 分钟 |

快速模式处理：

- 代码定位仅用 grep 关键词搜索，跳过 Graphify。
- 不查询参数库。
- 不做原型设计。
- 质量检查用轻量自检。

标准模式处理：

- 代码定位必须先执行 Graphify 策略A，再执行 Grep 策略B。
- 涉及参数、配置、院区差异时按需查询参数库。
- 原型设计按需触发。
- 质量检查自适应（简单场景轻量自检，复杂场景深度评审）。

复杂模式处理：

- 先进入 `brainstorming`，逐步澄清已确认范围、待确认问题、推荐方案方向、原型或流程边界、需要人工确认的关键点。
- 用户人工确认前，不生成正式 `产品业务分析.md`，不回写 `Winning.Demand.Analysis`，不上传临时澄清稿。
- 用户确认后，再继续正式详细分析流程；继续到质量检查阶段时，必须执行 `gstack/autoplan` 深度评审。
- 如果用户不确认或信息不足，停止并输出待确认问题清单。
- 参数库必须查询。
- 原型设计必须执行。

## 4. 代码、图谱、参数和知识资料

### 4.1 代码定位

- 代码调研前先检查目标代码目录是否存在 `graphify-out/graph.json`；没有图谱是正常情况，不要求安装或现场构建。
- 必须在 `notes.md` 记录”图谱存在/不存在/异常、使用 Graphify 或回退 rg 的原因”，避免遗漏前置检查。
- 不为了本次分析现场构建 Graphify 图谱。
- 如果前端目录和后端目录相同，或一个目录包含另一个目录，只搜索一次共同根目录，再按命中文件类型区分前后端，不要重复扫描同一棵目录。
- 每轮最多提取 3-6 个业务关键词，优先使用客户原文中的菜单名、页面名、字段名、按钮名、参数名和唯一业务术语；不要把泛词（如”查询、保存、状态、审核、列表、详情、病案、病历”）单独作为第一轮搜索词。
- 代码结论只转成业务现状、字段来源、影响范围、参数策略和测试用例口径；不得在正文输出代码路径清单。

**定位策略（按优先级）**：

#### 策略A：Graphify 图谱查询（标准/复杂模式必执行，快速模式跳过）

> ⚠️ **强制要求**：标准/复杂模式下，进入策略B（grep）之前**必须先执行本步骤**，无论图谱是否存在。

> ⚠️ **中文关键词必须先映射**：graphify 图谱节点均为英文代码标识符，中文关键词直接查询必然返回空结果。

执行前先检查图谱是否存在（`graphify-out/graph.json`），不存在则在 `notes.md` 记录后直接进入策略B。存在则按以下4步执行。

> ⚡ **步骤1+步骤2 可并行启动**：步骤1（映射表查）仅读取本地 `chineseToEnglishMap` 配置，步骤2（grep 桥接 subagent）仅依赖已获取的中文关键词，两者互不依赖。可同时发起步骤1的映射表查和步骤2的 2 路 grep subagent，完成后合并双方标识符。

**步骤1：映射表查（快速路径）**

1. 读取 `config/codebase-paths.json` 中当前产品线节点的 `chineseToEnglishMap` 字段
2. 从需求标题、描述和业务关键词中提取中文术语，逐一查映射表
3. 收集所有映射到的英文标识符，用空格拼接为一个查询字符串
4. 在 `notes.md` 记录映射过程，格式：`中文关键词映射：病历→EmrRecord,Emr；会诊→Consultation,Consult`
5. 无论映射表是否有结果，**都必须继续执行步骤2**，不得跳过

**步骤2：Grep 桥接（必须执行，不可跳过）**

> ⚠️ **强制要求**：本步骤必须执行，即使步骤1的映射表已命中。映射表只覆盖已知高频术语，grep 桥接能发现映射表未覆盖的关联标识符，两者互补。

目的：从代码中自动发现与中文关键词相关的英文标识符。

> ⚡ **并行优化**：前后端代码搜索独立，启动 2 个 subagent 并行执行，汇总后统一提取标识符。（如果前后端目录相同，只启动 1 个 subagent 搜索共同根目录。）

1. **启动 2 路并行 subagent**（使用 `superpowers:dispatching-parallel-agents`）：
   - **Subagent A（后端）**：在 `codeBasePath` 下用中文关键词 grep 搜索，返回命中行列表
   - **Subagent B（前端）**：在 `frontendCodeBasePath` 下用中文关键词 grep 搜索，返回命中行列表
   - 两路 subagent 的搜索关键词、排除目录等参数保持一致
2. **汇总结果**：等待两路 subagent 完成后，合并前后端命中行
3. 从合并命中行中提取 CamelCase 英文标识符（Java 类名、Vue 组件名、方法名等）
4. 去掉常见后缀（DTO、VO、PO、Controller、ServiceImpl、Service、Mapper、Repository、Component、Module），得到”基名”
5. 统计每个基名出现在多少个不同文件中
6. 取出现次数 ≥ 2 的基名作为主干词（降序排列，最多取3个）
7. 在 `notes.md` 记录桥接过程，格式：`grep桥接：关键词'病历'→ 基名 EmrRecord(12文件), EmrConsult(8文件), PatientEmr(3文件)`
8. **合并步骤1和步骤2的标识符**：将映射表命中的标识符与 grep 桥接发现的主干词去重合并，作为步骤3的输入
9. **步骤1和步骤2均无标识符** → 在 `notes.md` 记录”映射表和grep桥接均未发现有效标识符”，进入步骤4

**步骤3：Graphify 关系追溯**

目的：用步骤1和步骤2合并后的英文标识符，通过 Graphify 获取代码关系网络。

> ⚡ **并行优化**：分两轮执行。第一轮所有标识符的 `query_graph` 并行发起；第二轮 `get_neighbors` 和 `shortest_path` 并行发起（两者互不依赖）。

**第一轮（并行）— query_graph**：

1. 对步骤1和步骤2合并后的每个主干词，同时发起 `query_graph` 查询
2. 所有 `query_graph` 调用互不依赖，通过 MCP 工具或 Bash CLI 并行执行
3. 收集所有查询结果中的关键节点（Controller、Service、核心 Entity 等）

**第二轮（并行）— get_neighbors + shortest_path**：

4. **get_neighbors**：对第一轮结果中发现的每个关键节点，同时发起邻居查询，展开调用链
5. **shortest_path**：当需求涉及多个概念时（如”会诊”+”计费”），对每个概念对同时发起最短路径追溯。此步骤与 get_neighbors 互不依赖，可并行执行

**汇总**：

6. 将查询结果记录到 `notes.md`，标注使用方式（MCP/CLI）
7. Graphify 调用异常时在 `notes.md` 记录异常信息，不阻塞流程，进入步骤4

Graphify 调用方式（优先使用 MCP 工具）：
- **MCP 工具**（常驻模式，查询 ~0.5s）：使用 `mcp__graphify__query_graph`、`mcp__graphify__get_neighbors`、`mcp__graphify__shortest_path`、`mcp__graphify__get_node`
- **降级 Bash CLI**（MCP server 未启动时，查询 ~8-10s）：`PYTHONIOENCODING=utf-8 python -m graphify query “<英文标识符>”`

**步骤4：降级兜底**

Graphify 查不到有效节点或关系不足时，用 grep 全量搜索结果继续分析：
- 在 `notes.md` 记录”Graphify 未返回有效结果，降级使用grep全量搜索”
- 进入策略B，但 grep 搜索可复用步骤2已收集的命中行

#### 策略B：Grep 搜索（所有模式）

> ⚡ **并行优化**：前后端代码库独立，启动 2 路并行 subagent，搜索结束后统一汇总结果。（如果前后端目录相同，只启动 1 个 subagent 搜索共同根目录。）

1. **启动 2 路并行 subagent**，用 `rg` 搜索业务关键词（中文术语 + 英文类名）；如果 `rg` 不存在，改用 grep：

   **Subagent A（后端搜索）** — 指定搜索目录为 `codeBasePath`，至少覆盖：
   - Controller、Service、ServiceImpl、DTO、VO、Entity/PO、Mapper、Repository
   - 参数/配置常量/枚举、字典、SQL/MyBatis XML
   - 与需求关键词相关的业务逻辑代码

   **Subagent B（前端搜索）** — 指定搜索目录为 `frontendCodeBasePath`，至少覆盖：
   - 菜单配置、路由定义、页面组件
   - 表格列定义、按钮/操作项、状态变量
   - 接口调用（API service）、文案/国际化 key
   - 共享组件的入参（如 `menuflag`、`activeType` 等）

2. **汇总结果**：等待两路 subagent 完成后，合并前后端搜索结果，统一判断实现状态

回退 `rg` 时必须分阶段搜索：
   1. **文件名/路径优先**：先用菜单名、页面名、字段名查文件名或路径，命中后只读取少量关键文件。
   2. **精确内容搜索**：再搜索带业务特征的中文词、字段英文名、接口名或参数名。
   3. **局部扩展**：只在已命中模块附近继续搜索相关 Controller/Service/组件/路由/Mapper。

- 单个关键词搜索结果过多时，立即换更精确关键词或限定文件类型/目录，不要继续读取大批结果。

推荐 `rg` 约束：

```bash
rg -n --hidden --glob '!**/.git/**' --glob '!**/node_modules/**' --glob '!**/dist/**' --glob '!**/build/**' --glob '!**/target/**' --glob '!**/.gradle/**' --glob '!**/logs/**' --max-count 80 “<关键词>” <代码目录>
```

如需按文件类型收窄，可追加 `--glob '*.java'`、`--glob '*.vue'`、`--glob '*.ts'`、`--glob '*.js'`、`--glob '*.xml'`、`--glob '*.sql'`。Claude Code 环境中也应遵守同样原则：先用受限搜索定位候选文件，再读取关键片段，避免让工具自动做全仓库深度探索。

#### 实现状态判断

| 状态 | 判断依据 | 后续动作 |
|------|---------|---------|
| 已完整实现 | 代码中已有需求描述的全部功能 | 停止分析，报告已实现 |
| 参数配置可解决 | 发现现有参数可控制该行为 | 标注参数方案，停止分析 |
| 部分实现 | 实现了部分功能 | 标注已完成部分，分析增量 |
| 未实现 | 未找到相关代码 | 继续后续分析 |

### 4.2 参数

- 只有需求涉及参数、配置开关、默认值、院区差异化、兼容旧行为兜底，或代码/历史实现显示该能力由参数控制时，才查询参数。
- 查询前读取 `references/parameter-reading.md`。
- 病案参数可使用 `scripts/query_ma_parameter.py`；病历参数可使用 `scripts/query_inpatient_emr_parameter.py`。
- 参数结论只写业务信息：已有参数是否可复用、是否需要新增参数、默认值如何保持旧行为、影响范围和待确认项。
- 新增参数路径必须按系统现有参数路径格式推导：先查代码常量、参数表/CSV、历史分析文档和同类参数，优先复用既有路径层级与命名格式；不得按业务模块归属、菜单名称或个人理解直接编造参数路径。
- 新增参数编码按系统内置规则生成；除非现有参数资料或代码已明确给出，不要自行编造参数编码。

### 4.3 知识资料

- `Knowledge/` 仅在需要对比历史功能、旧口径、产品边界或复用功能时读取。
- 先用文件名、索引或关键词定位候选资料；默认只读取最相关的 1-3 个文件片段，不要为了“查资料”加载整个 `Knowledge/` 目录或无关大文件。
- 知识资料和代码冲突时，以当前代码和配置实现为事实基础，再说明差异。

## 5. 需求分析正文要求

正文写入 `Winning.Demand.Analysis`，本地源稿为 UTF-8 `产品业务分析.md`。正文应先总后分，前半部分让产品、研发、测试快速核对方向，后半部分承载设计细节、现状依据和影响范围。

Markdown 源稿必须严格按 `assets/product-business-analysis-template.md` 结构生成：

**第 1 章”背景”**：
- `1.1 业务诉求`：一句话总结客户核心诉求，格式：`客户希望[做什么]，以解决[什么问题]。`
- `1.2 业务现状`：一句话总结当前系统现状，格式：`当前系统[是什么状态/有什么功能/缺少什么]。`

**第 2 章”产品设计”**：
- 标题下必须先写 `核心功能：`，用一句话概括本需求要新增或修改的核心功能。
- `🔴 2.1 功能新增或修改`：表格列出功能变更。变更说明不得体现代码逻辑，重点体现界面修改（字段、按钮、区域、弹窗等）、功能逻辑修改（业务规则、流程调整、判断条件等）、参数修改。使用🔴标记突出，提醒研发和测试重点关注。
- `🔴 2.2 原型设计`：仅当有原型设计时输出。必须列出原型上每个字段的新增/修改详细信息，包含区域/页面、字段名称、变更类型、字段编码/接口字段、数据来源、显示条件、默认值、空值处理。使用🔴标记突出。
- `🔴 2.3 参数配置（如涉及）`：仅当涉及参数时输出。直接输出”新增参数”表格，不要输出参数分析过程。使用🔴标记突出，提醒研发和测试重点关注参数配置对系统行为的影响。
- `2.4 操作流程`：概要流程 + 详细步骤。涉及多步骤时先给概要流程图（用 `A -> B -> C`），再列详细步骤。

**第 3 章”测试用例”**：
- 基于角色与场景生成测试用例，供研发自测和测试人员参考。
- 表格列出：用例编号、角色、场景、操作步骤、预期结果、优先级。

**第 4 章”关联说明”**：
- `4.1 前置需求（如有）`：表格列出前置需求号、标题、关联说明。

**第 5 章”兼容性与风险”**（条件章节）：
- 仅当有兼容性影响或风险时输出，无实质内容时删除。

**禁止事项**：
- 不得随意增删章节或改变顺序
- 不得输出”参数分析过程”等分析过程记录
- 不得新增”验收标准”等模板中不存在的章节
- 条件章节无实质内容时直接删除，不要保留空表格或占位符

### 界面类需求补充项

- 页面入口/区域：在哪个页面、区域、卡片、页签、抽屉或弹窗展示。
- 原型设计及字段变更明细：必须在 2.2 节逐页面列出每个新增或修改的字段、按钮、控件，标明变更类型（新增/修改/删除）、变更前后差异、数据来源、显示条件。
- 按钮/操作清单：操作名、触发条件、点击后动作、是否复用现有接口、权限控制、禁用/隐藏规则。
- 测试用例可覆盖：每个新增字段、按钮和权限变化都能对应到测试用例。

## 6. 原型设计

触发条件：

- 新增界面。
- 界面布局重大修改。
- 配置界面修改较大。
- 用户交互流程变更。

处理规则：

- 原型设计按需触发，不是所有需求都输出原型。
- 原型必须先给用户确认，确认前不上传 TFS 附件。
- 用户确认后，导出原型并上传 TFS 附件；正文只记录原型附件名称、页面范围和核心变更点。
- 原型设计细则见 `scripts/prototype-design.md`。

## 7. 质量检查

轻量自检适用于快速模式，至少检查：

- 业务诉求、现状、推荐方案是否一致。
- 功能新增或修改、原型设计字段变更明细、参数配置三项是否闭环。
- 字段、按钮、入口、权限、空值、导出和测试用例是否闭环。
- 参数默认值是否保持旧行为。
- 是否存在未决词但未输出待确认问题。
- 是否误把代码路径、工具日志或过程记录写入正文。

复杂模式必须使用 gstack/autoplan 深度评审；标准模式按需使用。如果工具不可用、当前环境会触发大范围扫描，或需求已经通过轻量自检和人工确认闭环，则回退增强自检。深度评审范围限定为：

- 业务完整性。
- 方案一致性。
- 功能变更、原型字段变更明细、参数配置是否完整。
- 测试用例是否覆盖核心场景和边界条件。
- 影响边界是否可测试。
- 待确认问题是否遗漏。
- 正文是否混入代码路径、工具日志、过程记录或空章节。

gstack/autoplan 深评发现待确认项时：

- 先形成编号问题清单，像 `brainstorming` 一样逐项向用户澄清；优先一次只问 1-3 个最阻塞的问题。
- 用户答复后，将确认结论并入 `产品业务分析.md`，并以该 Markdown 为源稿重新回写 `Winning.Demand.Analysis`；不得只上传或更新 Markdown 附件。
- 用户无法确认且该问题影响方案、边界或测试用例判断时，停止在草案阶段，不回写 `Winning.Demand.Analysis`。
- 深度评审默认最多一轮；除非发现明确业务结论错误，不要反复调用评审。

gstack/autoplan 不可用时，回退增强自检并在最终摘要说明。

## 8. 回写与完成

默认回写：

1. 使用 MCP 文件路径能力从当前最新版 `产品业务分析.md` 更新 `Winning.Demand.Analysis`；如果 autoplan 或人工澄清后改过 Markdown，必须重新执行本步骤。
2. 回写后重新读取工作项一次，确认关键中文、表格和正文结构。
3. 追加 `AI-ANALYSIS` 和 `EMR-AI-ANALYSIS-V2` 标签，不覆盖原标签。
4. 上传最终版 `产品业务分析.md` 前先检查现有附件：同一轮已上传且本次只改表头、排版、回写同步或未证实修订时，不再上传；确有业务内容变更时优先覆盖同名附件，附件注释必须说明业务变更点。
5. 如有用户确认后的原型，上传原型附件。
6. 检查并创建 `需求分析_<需求号>` 子任务，已存在则跳过；检查任务时优先读取当前需求的直接子项，不做跨项目大范围 WIQL 搜索。

不做：

- 不追加时限讨论。
- 不上传复杂需求临时澄清稿。
- 不生成研发实现方案。
- 不创建开发分支。

## 9. 失败和跳过

直接停止：

- 无 TFS 需求号。
- TFS MCP 不可用。
- TFS 读取不到工作项。
- 产品名称无法匹配产品线且无默认产品线。
- 代码路径配置缺失或目录不可访问。

允许继续但必须记录：

- 非关键附件下载失败。
- Graphify 图谱不存在或异常，已回退 `rg`。
- 参数查询脚本失败，已回退 CSV 或跳过。
- gstack/autoplan 不可用，已回退增强自检。
- 分析任务创建失败，最终摘要说明人工处理动作。

## 10. 文件与参考资料

| 文件 | 用途 |
| --- | --- |
| `assets/product-business-analysis-template.md` | 生成产品业务分析 Markdown |
| `config/codebase-paths.json` | 选择病案/病历代码目录 |
| `references/parameter-reading.md` | 参数读取规则 |
| `references/tfs-integration.md` | TFS 读写和异常处理细则 |
| `references/Param/` | 参数资料 |
| `Knowledge/` | 病案/病历功能清单 |
| `scripts/download_tfs_embedded_images.py` | 下载 TFS 描述中的内嵌图片 |
| `scripts/query_inpatient_emr_parameter.py` | 查询病历参数 |
| `scripts/query_ma_parameter.py` | 查询病案参数 |
| `scripts/search_code.py` | 文本搜索辅助 |
| `scripts/update_demand_analysis.py` | Markdown 转 HTML 并回写需求分析字段的 fallback |
| `scripts/prototype-design.md` | 原型设计指南 |
| `scripts/create-analysis-task.md` | 自动创建需求分析任务说明 |
