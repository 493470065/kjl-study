# 产品业务分析

> **生成要求**：必须严格按照本模板结构生成内容，不得随意增删章节或改变顺序。条件章节（参数配置、原型、兼容性与风险）仅在涉及相关内容时输出，无实质内容时直接删除该章节，不要保留空表格或占位符。
>
> **禁止事项**：
> - 不得新增模板中不存在的章节（如"验收标准"、"产品分析概述"等）
> - 不得输出分析过程记录（如"参数分析过程"、"代码定位过程"等）
> - 不得改变章节顺序或编号
> - 条件章节无实质内容时必须删除，不得保留空表格

## 1. 背景

### 1.1 业务诉求

> **要求**：必须用一句话总结客户的核心诉求，不得长篇大论。格式：`客户希望[做什么]，以解决[什么问题]。`

### 1.2 业务现状

> **要求**：必须用一句话总结当前系统的现状，不得长篇大论。格式：`当前系统[是什么状态/有什么功能/缺少什么]。`


## 2. 产品设计

核心功能：

### 🔴 2.1 功能新增或修改

> **说明**：本章节为重点变更内容，研发和测试需重点关注。
>
> **格式要求**：必须用层级结构（`**主题N：XXX**` 粗体引导 + 列表展开），**禁止用平铺表格**。便于产品在 TFS 附件中直接手工修改（表格增删行需调列宽、单元格换行不一致，改为列表后每条改动独立成块，便于复制、修改和评审）。
>
> **变更说明要求**：变更说明中不得体现代码逻辑（如调用哪个Service、修改哪个方法），重点体现功能层面的变更，包括界面修改（新增/调整/删除哪些字段、按钮、区域、弹窗等）、功能逻辑修改（业务规则变化、流程调整、判断条件变更等）、参数修改（新增/调整参数及其行为）。

**主题一：（改造主题名称，如"病历辅助区数据展示强化"）**
- 变更动作描述：做什么 + 怎么做，关键对象（字段/按钮/入口/校验规则/接口名）在句中加粗。
- 第二条变更动作描述，同样在句中加粗关键信息。

**主题二：（改造主题名称）**
- 变更动作描述，关键信息在句中加粗。

> 改造点 ≤ 2 个时，可只保留一个主题或直接用列表展开，不必强行分层。

### 🔴 2.2 原型设计

> **输出条件**：仅当有原型设计时输出此章节。无原型时直接删除此章节，不要保留空内容或占位符。
>
> **说明**：本章节为重点变更内容，研发和测试需重点关注原型上的字段变更。

**原型文件**：

**原型字段变更明细**：

| 区域/页面 | 字段名称 | 变更类型 | 数据来源 | 显示条件 | 默认值 | 空值处理 | 备注 |
|-----------|---------|---------|---------|---------|--------|---------|------|
|  |  | 新增/修改/删除 |  |  |  |  |  |

**变更点说明**：
1.
2.

### 🔴 2.3 参数配置（如涉及）

> **说明**：本章节为重点变更内容，研发和测试需重点关注参数配置对系统行为的影响。
>
> **格式要求**：必须用列表形式，禁止使用表格。便于产品在 TFS 附件中直接手工修改参数值/默认值/说明。
>
> **参数前必须标注序号和类型**（新增/已有/修改），让多个参数能并列。**参数编码字段规则**：新增参数去掉参数编码字段（产品通过参数名称和路径定位即可，避免凭空给"建议编码"造成误导）；已有/修改参数保留参数编码字段，写业务可见的编码。

**新增参数示例**（无参数编码）：

- **1. 新增 参数名称：（参数的中文名称，如"手术医师必填校验开关"）**
  - 参数路径：（建议落入的菜单路径，如"数据校验配置"）
  - 建议默认值：（出厂默认取值）
  - 说明：（一句话说明用途、影响范围或生效条件）

**已有参数示例**（保留参数编码，本次不修改默认值，仅启用、引用或说明使用方式）：

- **2. 已有 参数名称：（参数的中文名称，如"住院号查询开关"）**
  - 参数编码：（业务可见的参数编码，如 `settingType`；不要写代码常量名）
  - 参数路径：（参数所在的菜单路径，如"病案查询参数设置"）
  - 当前默认值：（现有默认值）
  - 说明：（本次需求中的使用方式或生效条件）

**修改参数示例**（保留参数编码，本次修改默认值或取值范围）：

- **3. 修改 参数名称：（参数的中文名称，如"手术医师是否必填"）**
  - 参数编码：（业务可见的参数编码，如 `operationDoctorRequired`）
  - 参数路径：（参数所在的菜单路径）
  - 当前默认值：（现有默认值）
  - 建议值：（本次需求建议的取值）
  - 说明：（一句话说明修改原因、影响范围或生效条件）

### 2.4 操作流程
概要流程：

1.
2.
3.

## 3. 测试用例

> **说明**：基于角色与场景生成的测试用例，供研发自测和测试人员参考。

| 用例编号 | 角色 | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---------|------|------|---------|---------|--------|
| TC-001 |  |  |  |  | 高/中/低 |

## 4. 关联说明

### 4.1 前置需求（如有）
| 需求号 | 标题 | 关联说明 |
|--------|------|---------|
|  |  |  |

## 5. 兼容性与风险
> **输出条件**：仅当有兼容性影响或风险需要说明时输出此章节。无实质内容时直接删除此章节，不要保留空表格或占位符。

### 5.1 兼容性
- 是否影响其他医院/院区/角色：
- 是否影响旧版本/升级：
- 是否影响历史数据或存量配置：
- 是否需要新增参数：

### 5.2 风险与规避
| 风险 | 影响 | 规避方案 |
| --- | --- | --- |
|  |  |  |
